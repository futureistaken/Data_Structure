# modules are the codes that are writen in another file and i want to inport it to my current file
# it can have functions

import BasicTranslator


print(BasicTranslator.translate("haha"))

# google list of python modules : these are the already writen python modulas that are exist and writen before

# two type of modules built-in and external

# installing python modules " pip install python-docx " python-docx is the name of the modules